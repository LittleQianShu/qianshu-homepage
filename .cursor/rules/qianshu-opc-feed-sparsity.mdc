---
description: OPC Feed 少写、写有营养的阶段，禁止每轮对话记一条
alwaysApply: true
---

# 少记、记阶段

默认**不写** OPC Feed。禁止「每条对话 / 每做对一题 / 每数出一个数」就 `events_create`。

## 可以写（且挂在当天学习主线下）

同一阶段只写**一条**子事件，合并该阶段里的多个小点：

- 他自己能复述的一个新概念（含原话、类比、易错点）
- 一次练习做到能看见结果的阶段（文件 + 现象，不是中间每一步）
- 卡住之后真正想通了（困惑 → 转折 → 新理解）

## 不要写

- 引导中的确认、重问、鼓励、闲聊
- 流水观察：「有四个按钮」「第一题是 100」这类一眼能看见的事
- 同一专题里连续做对的每一题（收成「做完这一组」一条即可）
- 打开页面、重复讲解、纯排错过程中的每一步

## 数量

同一天同一主线下，阶段子事件通常 **0–3 条**，不要超过 **5 条**。拿不准就先不写，或并进已有子事件（`events_update`）而不是再开一条。
